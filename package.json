{
  "name": "thessaloniki-bus-map",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Interactive map of Thessaloniki public transport (OSETH + Metro) — GTFS + OSM + HMM map matching",
  "scripts": {
    "download": "bash pipeline/download.sh",
    "build": "node --max-old-space-size=6144 pipeline/build.mjs --all --tram M1",
    "report": "node pipeline/report-gaps.mjs",
    "serve": "node pipeline/serve.mjs 8126"
  }
}
